# Agent 状态简报 — 2026-05-27

> 这是给下一个 agent 看的。读完你就知道该干什么，不用重头问用户。

## 当前进度

- **项目**: 单品种训练流程（A 品种月度稳定性筛选实验）
- **已完成**: 
  - FactorFilter 封装（`run_full_pipeline` + 跨品种比例限制）
  - notebook Cell 4/5 简化（只保留配置）
  - `plot_factor_drift.py` 新增 `plot_factor_comparison`
- **搁置**: **月度稳定性筛选的 `boundary_drift_score` 方向效果不理想，用户已叫停**
- **状态**: 代码已改，**尚未跑通训练和回测对比**

## 今天做了什么（代码变更摘要）

### 修改 1：FactorFilter.py 重构
- **问题**: Cell 4 代码太多（5800 字符），筛选链手动调用三步
- **修复**: 新增 `run_full_pipeline(factor_df, config)`，一键跑完整筛选链，返回 summary DataFrame
- **新增**: `cross_symbol_select(symbol, max_cross_ratio)` — 跨品种因子比例限制（按重要性保留 top N）
- **新增**: 各方法 `verbose` 参数，支持静默输出
- **文件位置**: `/home/future_commodity/function_future/FactorFilter.py`

### 修改 2：月度稳定性筛选探索（已搁置）
- **尝试 1**：`mean_r2` + `std_r2` 硬门槛（已注释，保留计算代码）
- **尝试 2**：`p95_r2` / `p05_r2` 边界漂移（已注释）
- **尝试 3**：`high_coverage_r2` / `low_coverage_r2` 极端值覆盖率漂移（已注释）
- **尝试 4**：`boundary_drift_score` — 综合指标，计算每月 P95/P05 在历史分布中的排名偏离度
- **结论**: `boundary_drift_score` 对整体平移型漂移不敏感（如 `FAC_CORR_PVOL_RET_Volraiseap` score=0.15，刚好卡门槛），**用户决定搁置此方向**
- **当前 `is_stable` 判定**: 只保留了 `mean_r2 <= mean_r2_thresh`（其余指标已注释掉，但 `stability_df` 里还保留这些列供参考）

### 修改 3：notebook 简化
- **Cell 4**: 从 5800 字符 → 1500 字符，只保留 `pipeline_config` + `run_full_pipeline()` + `display(summary)`
- **Cell 5**: 从 5100 字符 → 500 字符，一行调用 `plot_factor_comparison`
- **文件位置**: `/home/strategy_res/single/dce_农/20250701/A_2025-07-01_v0_monthly_stability.ipynb`

### 修改 4：批量评估脚本
- **新增**: `batch_factor_eval.py` — 批量跑所有品种的月度稳定性评估
- **新增**: `A_stability_clean.csv` — A 品种 1865 个因子的 round 后评估表
- **文件位置**: `/home/single_factor_eval/`

### 修改 5：skill 记录
- **新增**: `ipynb-format-guard` — 记录修改 `.ipynb` 时的格式保护规范（防止 cell source 被压成单行）
- **文件位置**: `/home/.agents/skills/ipynb-format-guard/SKILL.md`

## 关键待办（按优先级）

- [ ] **跑新版 notebook（A 品种）的训练+回测**，和原版对比夏普/IC/净值曲线
- [ ] **决定月度稳定性筛选的最终方案**：用户已搁置 `boundary_drift_score`，需确认是否回退到 `mean_r2` 单指标，或完全不用月度稳定性
- [ ] **调整阈值**：若用 `mean_r2` 单指标，确认 `mean_r2_thresh` 取值（当前 0.8）
- [ ] **扩展到 M/P/Y 等其他品种**
- [ ] **批量高清漂移图**：用户要求 figsize=14×7, dpi=150，带 P95/P05 线（之前已删，需重新跑）

## 如果你要继续做，建议顺序

1. **先和用户确认**：月度稳定性筛选还要不要？如果要，用哪个指标（mean_r2 单指标 / 其他）？
2. **跑训练+回测**：新版 vs 原版对比（核心任务）
3. **阈值调参**：根据对比结果调整 `mean_r2_thresh`
4. **批量画图**：确认方案后再跑高清图

## 已知陷阱（别踩）

1. **修改 `.ipynb` 必须用 list of strings**：禁止 `''.join(source)` + `split('\n')`，详见 `ipynb-format-guard` skill
2. **notebook Cell 4 配置目前只有 `boundary_drift_thresh`**：如果回退到 `mean_r2`，需要更新配置字段
3. **FactorFilter.py 里 `is_stable` 判定目前是 `boundary_drift_score <= thresh`**：若改方案需同步改判定逻辑
4. **跨品种因子比例限制 `cross_symbol_select` 已生效**：在 `run_full_pipeline` 中 corr_select 之后执行

## 路径速查

| 用途 | 路径 |
|------|------|
| 月度稳定性 notebook | `/home/strategy_res/single/dce_农/20250701/A_2025-07-01_v0_monthly_stability.ipynb` |
| 原版 notebook | `/home/strategy_res/single/dce_农/20250701/A_2025-07-01_v0.ipynb` |
| FactorFilter | `/home/future_commodity/function_future/FactorFilter.py` |
| 批量评估脚本 | `/home/single_factor_eval/batch_factor_eval.py` |
| A 品种评估表 | `/home/single_factor_eval/A_stability_clean.csv` |
| 可视化模块 | `/home/strategy_res/single/dce_农/20250701/plot_factor_drift.py` |
| 问题记录 | `/home/notes/factor/2026-05-27_Volraiseap_modifier_structural_drift.md` |

### 修改 6：回退 `is_stable` 到 R² 硬门槛
- **问题**: `boundary_drift_score` 对整体平移不敏感（如 `FAC_CORR_PVOL_RET_Volraiseap` score=0.155 仍通过，但均值漂移严重 mean_r2=0.916）
- **修复**: 
  - `is_stable` 改回 4 项 R² 硬门槛：`mean_r2<=0.8`, `std_r2<=0.7`, `p95_r2<=0.7`, `p05_r2<=0.7`
  - `monthly_stability_select` 淘汰原因打印同步更新
  - `run_full_pipeline` summary note 同步更新
  - notebook Cell 4 `pipeline_config` 同步替换 `boundary_drift_thresh` → R² 参数
- **文件位置**: `/home/future_commodity/function_future/FactorFilter.py`, `/home/strategy_res/single/dce_农/20250701/A_2025-07-01_v0_monthly_stability.ipynb`

### 修改 7：新建 V1 整洁版 notebook + 封装 pipeline
- **问题**: 原 notebook cell 代码冗长，回测参数散装，交易时间 bar 数硬编码
- **修复**:
  - `config_info.yaml` 新增 `bars_per_day`（A/B/C/CS/M/Y/P=345, LH=225），`get_instrument_config` 可直接读取
  - 新建 `pipeline_v1.py` 封装：数据加载、资金计算、pretrain、因子筛选、训练、回测
  - 新建 `A_2025-07-01_v1_clean.ipynb`（13 cells），每 cell 极短，只保留配置或一行调用
  - 回测参数集中为 `BT_PARAMS` dict，与 dce农_独立版本对齐
  - 初始资金改为 `calc_initial_capital`（40% 保证金比例，与 File2 一致并 print）
- **文件位置**: 
  - `/home/strategy_res/single/dce_农/20250701/pipeline_v1.py`
  - `/home/strategy_res/single/dce_农/20250701/A_2025-07-01_v1_clean.ipynb`
  - `/home/future_config/basic_config/config_info.yaml`

## 参数

- `mean_r2_thresh`: `0.8`
- `std_r2_thresh`: `0.7`
- `p95_r2_thresh`: `0.7`
- `p05_r2_thresh`: `0.7`
- `max_cross_ratio`: `0.1`（跨品种因子比例上限）
- `cut_num_1`: `300` / `cut_num_2`: `200`（重要性筛选）
- `feature_num_limit`: `300`, `corr_limit`: `0.9`（相关性筛选）
