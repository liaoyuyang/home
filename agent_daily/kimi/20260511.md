# Agent 状态简报 — 2026-05-11（最终版）

> 时间：2026-05-12 早晨。用户已回到工作，夜间事件已收尾。

## 当前进度

- **验证范围**：8 个 DCE 农产品期货 — A, B, C, CS, M, Y, P, LH
- **已验证**：P（p2605），7 日窗口 2026-03-17 ~ 2026-03-23
- **Market（1min）**：对齐，max_diff = 0 ✅
- **Factor**：大部分对齐；发现 **ILLIQ 因子在研究环境大量为 NaN**，LVC 有微小数值差异（tolerance 内）
- **夜间 Pipeline**：❌ 未自动启动（approval 被拒绝），用户决定手动后台运行

## 昨天做了什么（代码变更摘要）

### 修改 1：tick_factors.py — 11 个因子消除 diff() 依赖
- **问题**：ILLIQ/MPB/CORR_PVOL_RET 等因子使用 `TotalTradeValue.diff()` / `TotalTradeVolume.diff()`，在跨日/跨批次时产生负跳变，导致大量 NaN
- **修复**：将以下 11 个因子的 diff() 依赖改为直接使用 `turnover` / `volume`（单笔值）
  - ILLIQ, MPB, CORR_PVOL_RET, STREN, VOL_FLU
  - QUA, shortQUA, midQUA, Price_Divergence, MFI, CMF
- **文件**：`/home/future_commodity/function_future/tick_factors.py`

### 修改 2：update_mkt_data.py / prepare_mkt_data.py — 索引修复
- **问题**：`level2_all` feather 文件保存了非 0 起始的索引
- **修复**：在 `drop_duplicates` 后增加 `.reset_index(drop=True)`
- **文件**：
  - `/home/future_commodity/prepare_data/prepare_mkt_data/update_mkt_data.py`
  - `/home/future_commodity/prepare_data/prepare_mkt_data/prepare_mkt_data.py`

### 修改 3：数据质量根因定位
- **根因**：`level2_all` 合并文件存在**新旧数据混合**——旧批次 `turnover` 是累计值，新批次是单笔值
- **确认**：`level2/` 源文件中 `turnover` 为单笔值，`level2_all` 合并文件中同一日期同一合约的 `turnover` 为累计值
- **结论**：必须全量删除 `level2_all` 后重建，不能仅靠增量更新

### 修改 4：新增 rebuild_level2_all_dce农.py（原脚本基础上修改）
- **与原脚本的区别**：
  1. `symbol_lst` 改为 `["A", "B", "C", "CS", "M", "Y", "P", "LH"]`（DCE 农产品）
  2. **删除了 `process_dates_with_joblib` 调用**，直接从已有的 `level2/` 合并到 `level2_all/`，跳过重新生成 `level2/` 的步骤（已验证 `level2/` 的 turnover/volume 为单笔值，无需重跑）
- `process_contract`、`process_instrument`、`agg_dict` 等后续逻辑与原脚本完全一致
- **文件**：`/home/future_commodity/prepare_data/prepare_mkt_data/rebuild_level2_all_dce农.py`

### 修改 5：factor_cross_index.ipynb → .py
- 转为 Python 脚本，便于后台运行
- **文件**：`/home/future_commodity/prepare_data/prepare_factor/dce_农/factor_cross_index.py`

## 数据清理状态（已完成）

以下旧数据**已全部删除**， pipeline 运行后会重新生成：
- `level2_all/{A,B,C,CS,M,Y,P,LH}/` — 8 个品种的旧合并数据（含累计值残留）
- `factor/{A,B,C,CS,M,Y,P,LH}/t2t/` — 8 个品种的旧 t2t 因子文件

## 用户下一步计划（5.12 待办）

1. **手动后台跑 pipeline**（用户自己执行）：
   ```bash
   cd /home/future_commodity/prepare_data/prepare_mkt_data
   python rebuild_level2_all_dce农.py
   cd /home/future_commodity/prepare_data/prepare_factor
   python calc_factors_dce农.py
   cd /home/future_commodity/prepare_data/prepare_factor/dce_农
   python factor_cross_index.py
   cd /home/future_commodity/prepare_data/prepare_factor
   python make_main_factor_dataframe_dce农.py
   ```
   或一键运行：`bash /home/agent_daily/run_pipeline_20260511.sh`

2. **核对实盘因子**（用户今天要做的主线工作）

3. **验收 t2t 因子重算结果**：验证 ILLIQ 等 11 个因子的 null_ratio 是否恢复正常

## 已知陷阱（别踩）

- **`level2_all` 不要增量更新**：当前增量更新逻辑会导致新旧数据混合，必须全量删除后重建
- **`turnover` 字段含义**：`level2/` 源文件中 `turnover` 是单笔成交额，`TotalTradeValue` 是累计成交额。`level2_all` 必须保持一致（均为单笔）
- **严禁修改原始数据源**：`/mnt/Data/future/decode_csv_dce/` 及其子目录绝对不可动
- **m2m/t2m 因子无需重算**：文件未删除，calc_factors_dce农.py 会自动跳过；只有 t2t 被删除后重算

## 路径速查

| 用途 | 路径 |
|------|------|
| rebuild_level2_all_dce农.py（重建用） | `/home/future_commodity/prepare_data/prepare_mkt_data/rebuild_level2_all_dce农.py` |
| 一键 pipeline 脚本 | `/home/agent_daily/run_pipeline_20260511.sh` |
| calc_factors_dce农.py | `/home/future_commodity/prepare_data/prepare_factor/calc_factors_dce农.py` |
| factor_cross_index.py | `/home/future_commodity/prepare_data/prepare_factor/dce_农/factor_cross_index.py` |
| make_main_factor_dataframe_dce农.py | `/home/future_commodity/prepare_data/prepare_factor/make_main_factor_dataframe_dce农.py` |
| tick_factors.py（ILLIQ/LVC 定义） | `/home/future_commodity/function_future/tick_factors.py` |
| level2 源文件 | `/mnt/Data/writable/liaoyuyang/data/level2/{SYMBOL}/{date}_{contract}.feather` |
| level2_all 合并文件 | `/mnt/Data/writable/liaoyuyang/data/level2_all/{SYMBOL}/{contract}.feather` |
| 研究因子文件 | `/mnt/Data/writable/liaoyuyang/factor/{SYMBOL}/t2t/{contract}/` |

## 参数

- TOL_ABS = 0.01, TOL_REL = 0.05, TAIL_N = 8
- Session 保留区间：09:10-10:05, 10:40-11:20, 13:40-14:50, 21:10-22:50
