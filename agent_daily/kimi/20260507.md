# 2026-05-07 Work Log — Data Reconciliation (Factor + Market Data)

## 1. What We Did Today

### 1.1 Built a reconciliation notebook
- File: `/home/strategy_PAMY_dev/check_history_factor.ipynb`
- Purpose: compare **realtime** (from `files/`) vs **research** (`all_factor.feather` + `1min/{contract}.feather`) for dates **2026-03-17 ~ 2026-03-23**.
- The notebook has two major sections:
  - **Market data check** (1min OHLCV + tick drill-down)
  - **Factor check** (simulate `_init_history`, compute all factors, align with research, diff stats + plots)

### 1.2 Found and fixed a bug in `calc_recent_data.py`
- **Issue**: duplicate ticks in raw decode_csv caused volume/turnover to be double-counted in the last minute of the morning session (11:30).
- **Fix**: added `drop_duplicates(subset=['datetime','last_price','volume','turnover','open_interest'], keep='first')` in `pre_resample_data`.
- **Impact**: after regenerating `files/`, 11:30 volume diff should disappear.

### 1.3 Key finding from market-data check (P/p2605)
- `open/high/low/close/open_interest`: **perfect match** (max_diff = 0).
- `volume/turnover`: mostly match, only diff at **11:30** morning close minute (due to duplicate ticks above).
- **No missing bars** when filtered by `trade_date` (night session 03-23 21:00-23:00 has `trade_date = 2026-03-24`, so it is excluded correctly).

## 2. Research Environment Flow (source of truth)

```
/mnt/Data/future/decode_csv_dce/  (raw tick from exchange)
    |
    v
/home/future_commodity/prepare_data/prepare_mkt_data/prepare_mkt_data.py
    |  reads raw tick -> filters last_price>0 -> aggregates to 1min -> saves
    v
/mnt/Data/writable/liaoyuyang/data/1min/{SYMBOL}/{contract}.feather
/mnt/Data/writable/liaoyuyang/data/level2_all/{SYMBOL}/{contract}.feather
    |
    v
/home/future_commodity/prepare_data/prepare_factor/
    |  calc_factors_dce农.py         (min factors + tick factors)
    |  factor_cross_index.ipynb       (cross-symbol factors)
    |  make_main_factor_dataframe_dce农.py  (concat all factors per main contract)
    v
/mnt/Data/writable/liaoyuyang/factor/{SYMBOL}/all_fac/all_factor.feather
```

- **Main contract table**: generated by `get_config_new.py` + `load_main_contract.ipynb`.
- **Trade-date rule**: night session (21:00-23:00) belongs to **next** trade_date.
- **Deduplication**: research `level2_all` already drops duplicate ticks.

## 3. Realtime Environment Flow

```
/home/strategy_PAMY_dev/
    |
    +-- calc_recent_data.py
    |       reads /mnt/Data/future/decode_csv_dce/ (same raw source)
    |       -> pre_resample_data() -> files/{contract}_tick.csv + _min.csv
    |
    +-- strategies.py  (generate_factor_dataframe_*)
    +-- data_function.py (Factor_generator, load_fac_df_old)
    +-- orchestrator.py (ProcessPoolExecutor, real-time loop)
```

- `calc_recent_data.py` is currently **pinned to `end_date = "2026-03-23"`** for data-recon purposes.
- The `_init_history()` path calls `load_fac_df_old()` which loads `files/` tick/min and computes factors exactly like the notebook.

## 4. Known Differences / Risk Areas

| # | Area | Research vs Realtime | Status |
|---|------|----------------------|--------|
| 1 | **Tick dedup** | Research drops duplicates; realtime now fixed (2026-05-07) | **Fixed** |
| 2 | **Trade-date filtering** | Must use `trade_date` column, not `datetime.date` | **Fixed in notebook** |
| 3 | **Cross-symbol factors** | Research may miss data for some symbols (e.g. B contract not updated) | **Known acceptable** |
| 4 | **Round(8)** | Realtime does `.round(8)` at end of `generate_factor_dataframe_*` | Research may not; needs verification |
| 5 | **Data source** | Research uses `level2_all` (pre-processed); realtime reads raw decode_csv directly | Minor diff possible at boundaries (e.g. 11:30) |
| 6 | **Main contract** | Research switches contract daily; realtime uses **fixed** contract (e.g. p2605) | By design; OK for same-contract window |

## 5. Next Steps (TODO)

1. **Regenerate `files/` after dedup fix**
   - Run `calc_recent_data.py` to re-create `files/*_tick.csv` and `*_min.csv`.
   - Re-run the notebook and confirm 11:30 volume diff is gone.

2. **Extend recon to all 8 symbols**
   - Current notebook was tested on `P` only.
   - Run for `A, B, C, CS, M, Y, LH`.
   - Pay attention to cross-symbol factors involving missing-data symbols (e.g. B).

3. **Factor-level deep dive**
   - Identify which factors have highest diff ratio.
   - Drill down to raw tick/min data for those factors.
   - Check if diff is due to:
     - Data alignment (ffill vs other method)
     - NaN handling
     - `round(8)` truncation
     - Calculation formula differences between `function_future.min_factors` and `data_function.Factor_generator`

4. **Prediction-level check**
   - After factor alignment is clean, compute `pred_df` from aligned factors.
   - Compare `weighted` and `weighted_s` with research environment.

5. **Production readiness**
   - Once factor diff < 1% for all symbols, the realtime env is ready for live trading.
   - Document any intentionally accepted diffs (e.g. KDJ/zigzag).

## 6. Quick Reference

- **Notebook**: `/home/strategy_PAMY_dev/check_history_factor.ipynb`
- **Realtime data**: `/home/strategy_PAMY_dev/files/`
- **Research factor**: `/mnt/Data/writable/liaoyuyang/factor/{SYMBOL}/all_fac/all_factor.feather`
- **Research 1min**: `/mnt/Data/writable/liaoyuyang/data/1min/{SYMBOL}/{contract}.feather`
- **Research tick**: `/mnt/Data/writable/liaoyuyang/data/level2_all/{SYMBOL}/{contract}.feather`
- **Models**: `/home/strategy_PAMY_dev/models/{SYMBOL}/`
- **Config**: `/home/strategy_PAMY_dev/config.json`
