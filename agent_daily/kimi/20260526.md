# Agent 状态简报 — 2026-05-26

> 这是给下一个 agent 看的。读完你就知道该干什么，不用重头问用户。

## 当前进度

- **项目**: strat_lab（本地策略研究框架）
- **已完成**: 项目框架 + 01_sync_mkt_data.py + 03_calc_cross_factors.py + 时间列统一
- **正在修**: `build_all_factor` 逻辑错误（从直接读 mnt all_factor 改为底层重新拼）
- **状态**: 代码已重写，文档已写，**尚未跑通验证**

## 今天做了什么（代码变更摘要）

### 修改 1：新建 `src/factor_assembler.py`
- **问题**: `_load_single_factors` 直接读 mnt `all_factor.feather`，含全部 8 品种跨品种因子，导致分组隔离失效
- **根因**: mnt 侧的 all_factor 是全局拼好的，不能按分组裁剪
- **修复**: 新建 `FactorAssembler`，从底层 `m2m/t2m/t2t` 按合约读取并按主力合约表拼接，完全复刻 `make_main_factor_dataframe_dce农.py` 逻辑
- **文件位置**: `/home/strat_lab/src/factor_assembler.py`

### 修改 2：重写 `src/group_builder.py`
- **问题**: `_load_single_factors` 直接返回 mnt all_factor，跨品种因子列无法按分组隔离
- **修复**: `_load_single_factors` 改为实例化 `FactorAssembler` 并调用 `assemble()`；删除旧的有毒逻辑（直接读 mnt all_factor + 事后删列）
- **文件位置**: `/home/strat_lab/src/group_builder.py`

### 修改 3：修改 `pipeline/04_make_factor_df.py`
- **问题**: CLI 没有控制并行度的参数，和同事共用机器时无法限制 CPU
- **修复**: 新增 `--n-jobs` 参数，默认 1，透传给 `FactorAssembler`
- **文件位置**: `/home/strat_lab/pipeline/04_make_factor_df.py`

### 修改 4：复制 `config/factor_config.yaml`
- **问题**: `strat_lab/config/` 下没有因子配置文件，`FactorAssembler` 无法生成 t2t 因子名
- **修复**: 从 `future_config/basic_config/config_factor.yaml` 复制到 `strat_lab/config/factor_config.yaml`
- **文件位置**: `/home/strat_lab/config/factor_config.yaml`
- **风险**: 若 mnt 实际因子是用不同版本配置生成的，会导致大量因子名不匹配（静默跳过）

### 修改 5：写设计文档
- 写了 4 份文档到 `/home/strat_lab/docs/`：
  - `factor_assembler.md` — 最详细，逐方法说明 + 与参考代码对照
  - `group_builder.md` — 跨品种因子计算 + all_factor 拼接流程
  - `pipeline_04.md` — CLI 参数 + 前置依赖 + 与其他步骤关系
  - `data_flow.md` — 全局数据流图 + 01-05 每步输入输出

### 修改 6：更新 `AGENTS.md`
- 追加第 9 节：拼表原则、CPU 控制红线、因子名生成、合约过滤不可省略

## 关键待办（按优先级）

- [ ] **验证 `FactorAssembler` 能否正确拼出完整单品种因子表**（当前未跑通，用户说后面细改）
- [ ] **确认 `config/factor_config.yaml` 与 mnt 实际因子完全匹配**（风险：配置版本不一致导致列缺失）
- [ ] **跑通 `04_make_factor_df.py --group 油脂油粕`**（端到端验证）
- [ ] 接入 `FactorFilter` 多维度筛选到 `05_train.py`（当前只取了重要性 top 300，是简化版）
- [ ] 实现 `research/` 下的 factor_analysis, resample_study, cross_group_compare
- [ ] 评估是否需要 `float32` 降内存（t2t 约 1840 列，全量 float64 峰值约 6-8GB）

## 如果你要继续做，建议顺序

1. **先看文档**: 读 `docs/factor_assembler.md` 第 4 节（已知问题）和第 5 节（因子名生成），了解坑在哪
2. **验证配置匹配**: 对比 `config/factor_config.yaml` 中的 `tick_tick_fac.fac_func_lst + modifiers` 与 mnt 实际文件名（`mnt/factor/A/t2t/a2401/` 下的文件列表），确认笛卡尔积结果是否一致
3. **跑单品种测试**: `python -c "from src.factor_assembler import FactorAssembler; df = FactorAssembler('A', n_jobs=1).assemble(); print(df.shape)"`
4. **跑端到端**: `python pipeline/04_make_factor_df.py --group 油脂油粕`
5. **检查输出**: 看 `data/groups/油脂油粕/all_factor/` 下的 feather 文件 shape 是否合理

## 已知陷阱（别踩）

1. **t2t 因子名是笛卡尔积**: `base_functions (92个) × modifiers (20个)` 约 1840 个。若直接从 `fac_func_lst` 读取而不乘 modifiers，mnt 上一个因子都找不到，返回全空表
2. **合约过滤 merge 不可省略**: `_read_single_factor` 里的 `merge(contract_info)` 是参考代码的核心逻辑，省略会导致合约切换日数据重叠
3. **mnt all_factor 有毒**: 无论多方便，都不能直接读。它包含全部 8 品种的跨品种因子（如 A_B, A_C），会破坏分组隔离
4. **内存峰值 6-8GB**: t2t 约 1840 列 × 40 万行，float64 下很占内存。和同事共用机器时 n_jobs 必须显式控制（默认 1）
5. **因子配置版本风险**: `config/factor_config.yaml` 是从 `future_config/` 复制过来的。若 mnt 因子是用旧版/新版配置生成的，实际存在的因子名和配置文件会对不上

## 路径速查

| 用途 | 路径 |
|------|------|
| 项目根目录 | `/home/strat_lab` |
| 单品种因子拼表器 | `/home/strat_lab/src/factor_assembler.py` |
| 分组构建器 | `/home/strat_lab/src/group_builder.py` |
| CLI 拼表入口 | `/home/strat_lab/pipeline/04_make_factor_df.py` |
| 因子配置文件 | `/home/strat_lab/config/factor_config.yaml` |
| 分组产物目录 | `/home/strat_lab/data/groups/{group_name}/` |
| 设计文档 | `/home/strat_lab/docs/` |
| 代码风格约定 | `/home/strat_lab/AGENTS.md` |
| mnt 因子数据 | `/mnt/Data/writable/liaoyuyang/factor/{symbol}/` |
| mnt 主力合约表 | `/mnt/Data/writable/liaoyuyang/data/future_info/{symbol}/main_instrument.csv` |
| 参考拼表代码 | `/home/future_commodity/prepare_data/prepare_factor/make_main_factor_dataframe_dce农.py` |

## 参数

- `n_jobs` 默认值: `1`（和同事共用机器，必须显式提高）
- `single_factor_source` 默认值: `"mnt"`
- `cross_factor_source` 默认值: `"local"`


---

## 追加 — 下午：单品种训练流程讨论与月度稳定性筛选实现

### 当前焦点转移
- 用户明确表示**忘掉 strat_lab**，回到原有单品种训练流程
- 核心目标不变：找到一组在各个行情下都比较普适的因子组

### 今天做了什么（代码变更摘要）

#### 修改 7：新建 notebook `A_2025-07-01_v0_monthly_stability.ipynb`
- **问题**: 现有 `FactorFilter` 的筛选链全是全局统计量（IC、sharpe、importance、corr），没有检测因子在时序上的分布漂移。用户希望筛出来的因子在训练窗口内分布稳定，不发生整体偏移。
- **修复**: 在原有 A 品种 notebook 基础上，新建一个 notebook，**只改 Cell 4（因子筛选部分）**。
  - 新增 `calc_monthly_stability()` 函数：按月切片，计算月度均值/标准差，检测趋势性漂移
  - 允许单月跳变（合约切换导致的异常月），但禁止持续漂移（均值/标准差跨月趋势显著）
  - 筛选顺序调整为：标准筛选（不含 corr）→ 月度稳定性筛选 → corr_select
- **文件位置**: `/home/strategy_res/single/dce_农/20250701/A_2025-07-01_v0_monthly_stability.ipynb`
- **与原版差异**: 仅 Cell 4 不同，Cell 0-3（imports/配置/数据加载/预训练）和 Cell 5+（训练/回测）完全不变

### 训练流程确认（单品种版）

用户确认了以下流程：

1. **数据准备**: 加载 `all_factor.feather` + `main_{symbol}.csv` 收益率，merge 成 `main_fac_piv`
2. **预训练**: `Pretrainer.run_full_pretraining(type_lgb='reg')` → 输出 importance/corr/group/single_factor_eval
3. **FactorFilter 筛选链**（原版6轮）:
   - `factor_info_select` (nan_rate, mode_rate)
   - `importance_select_by_group` (有向/无向分别取 top，同名限制)
   - `ic_select` (IC 阈值)
   - `sp_select` (sharpe 阈值)
   - `day_cut` (日频开盘因子数量限制)
   - `corr_select` (相关性贪婪去冗余)
4. **正式训练**: `LGBMTrainer.train_kfold_v0()` → 输出模型
5. **回测**: `ModelBacktester`

### 关键讨论结论

| 话题 | 结论 |
|------|------|
 行情分层预训练（构思一） | 用户感兴趣，认为"看起来还可以"，但本次未落地 |
| 月度分布稳定性 | **已落地**，作为 FactorFilter 筛选链的新增环节 |
| 时间切片粒度 | 月度（不是固定步数），用户认为 1000 步太少 |
| 跳变容忍 | 允许合约切换导致的单月跳变，但禁止连续 3 个月异常或趋势性漂移 |
| 阈值暂定 | `max_outlier_ratio=0.25`, `trend_r2_thresh=0.4`, `slope_abs_thresh=0.01` |

### 新增待办

- [ ] **跑通 `A_2025-07-01_v0_monthly_stability.ipynb`**，验证月度稳定性筛选效果
- [ ] **调整月度稳定性阈值**：先跑一遍看 `mean_r2` 和 `outlier_ratio` 的分布，再人工定阈值
- [ ] **对比原版 vs 新版**：同一品种用两套筛选分别训练，看回测夏普/IC 是否有提升
- [ ] **行情分层预训练（构思一）**：若月度稳定性有效，再考虑叠加行情分层

### 如果你要继续做，建议顺序

1. **先跑新版 notebook**：打开 `A_2025-07-01_v0_monthly_stability.ipynb`，直接跑 Cell 0-4，看月度稳定性筛选淘汰了多少因子、淘汰原因是否合理
2. **看分布定阈值**：Cell 4 跑完后，`stability_df['mean_r2'].hist()` 和 `stability_df['outlier_ratio'].hist()` 看分布，调整 `trend_r2_thresh` 和 `max_outlier_ratio`
3. **端到端对比**：新版筛选后接 Cell 5（训练）和 Cell 6+（回测），和原版 notebook 的结果对比
4. **扩展其他品种**：参数调好后，复制到 M/P/Y 等其他品种的 notebook

### 已知陷阱（别踩）

1. **月度稳定性放在 corr_select 之前**：必须先做月度稳定筛选，再做 corr_select。如果反过来，corr_select 已经按重要性 greedy 选了 300 个，再做月度稳定可能只剩 200 个，但 corr 结构已经被破坏了
2. **月度切片包含合约切换月**：合约切换那一个月统计量异常是正常的，这是"跳变"而非"整体偏移"，已经被 `max_consecutive_outliers <= 2` 容忍
3. **t2t 因子天然波动大**：月度均值的 MAD 可能很大，导致 `outlier_mask` 过于宽松。如果跑完发现 t2t 因子几乎没被淘汰，可能需要收紧 `max_outlier_ratio`
4. **训练窗口太短**：如果训练窗口只有 3-4 个月，月度切片就没有意义（样本太少）。用户用的是 `train_end_date='2025-07-01'`，数据从 2021 年开始，应该够

### 路径速查（新增）

| 用途 | 路径 |
|------|------|
| 月度稳定性 notebook（A 品种） | `/home/strategy_res/single/dce_农/20250701/A_2025-07-01_v0_monthly_stability.ipynb` |
| 原版 notebook（A 品种） | `/home/strategy_res/single/dce_农/20250701/A_2025-07-01_v0.ipynb` |
| 单品种预训练模块 | `/home/future_commodity/function_future/pre_train.py` |
| 因子筛选器 | `/home/future_commodity/function_future/FactorFilter.py` |
| 单因子评估（IC/夏普） | `/home/future_commodity/function_future/single_fac_eval.py` |
| A 品种 all_factor | `/mnt/Data/writable/liaoyuyang/factor/A/all_fac/all_factor.feather` |
| A 品种收益率数据 | `/mnt/Data/writable/liaoyuyang/data/1min/active/main_A.csv` |
| 预训练输出目录 | `/mnt/Data/writable/liaoyuyang/factor_eval_commodity/2025-07-01/` |

### 参数（新增）

- `max_outlier_ratio`: `0.25`（最多 25% 月份异常）
- `trend_r2_thresh`: `0.4`（R²>0.4 认为有趋势性漂移）
- `slope_abs_thresh`: `0.01`（月度均值斜率阈值）
- `max_consecutive_outliers`: `2`（连续异常月不超过 2 个月）


---

## 追加 — 晚间：运行验证与 matplotlib 字体调试

### 运行结果观察

用户运行了 `A_2025-07-01_v0_monthly_stability.ipynb` Cell 0-5，关键发现：

#### 月度稳定性筛选结果
```
[月度稳定性] 共 1865 个因子, 806 个稳定
```
- **淘汰 1059 个因子（57%）**，力度比预期大
- 被淘汰的典型因子：
  - `FAC_CORR_PVOL_RET_Volraiseap`: 均值漂移 R²=0.916, 波动漂移 R²=0.768
  - `FAC_CORR_PVOL_RET_vwap`: 均值漂移 R²=0.909, 波动漂移 R²=0.855
  - `FAC_MOFI_kurtosis`: 均值漂移 R²=0.901, 波动漂移 R²=0.897
  - `FAC_STREN_kurtosis`: 均值漂移 R²=0.906, 波动漂移 R²=0.895
  - `FAC_CORR_PVOL_RET_max`: 均值漂移 R²=0.897, 波动漂移 R²=0.870

#### 筛选链全流程
```
标准筛选后:     327 个
月度稳定筛选后: 153 个  (干掉 53%)
corr_select 后: 134 个
最终分类: FAC=77, STK=0, 跨品种=37, 其他=20
```

#### 可视化验证
- 被筛掉因子的月度均值/标准差时序图**确实呈现明显线性上升趋势**
- `CORR_PVOL_RET_*` 类和 `*_kurtosis` 类因子漂移最严重
- 稳定因子对比组呈现水平直线，验证了筛选逻辑正确

### 问题与修复

| 问题 | 原因 | 修复 |
|------|------|------|
| Cell 5 SyntaxError | f-string 跨行被 JSON 拆断 | 重写 Cell 5，title 文本先赋值给变量再传 `set_title` |
| matplotlib 中文方块 | 系统无可靠中文字体；`legend` 参数名是 `prop` 不是 `fontproperties`；`set_ylabel` 缺右括号 | 安装 `fonts-wqy-zenhei`；彻底重写 Cell 5，所有中文显示位置显式传入 `fontproperties`/`prop` |

### 代码变更

- **Cell 4**: 无变更，运行正常
- **Cell 5**: 彻底重写（第3版），修复所有字体问题
  - 使用 `fm.FontProperties(fname="/usr/share/fonts/truetype/wqy/wqy-zenhei.ttc")`
  - `set_title`/`set_ylabel`/`set_xticklabels`/`suptitle` 均传入 `fontproperties=fp`
  - `legend` 传入 `prop=fp_small`

### 待办状态更新

- [x] **跑通 `A_2025-07-01_v0_monthly_stability.ipynb`** — Cell 0-5 已跑通
- [x] **确认被淘汰因子确实存在漂移** — 时序图已验证
- [ ] **调整月度稳定性阈值** — 用户说"后面细改"，当前阈值（R²>0.4）淘汰力度很大
- [ ] **对比原版 vs 新版回测** — 明天继续
- [ ] **扩展其他品种** — M/P/Y 等

### 明天建议

1. **先跑训练+回测**：新版 134 个因子跑 Cell 5 之后的训练和回测，和原版 300 个因子的结果对比
2. **阈值实验**：尝试放宽 `trend_r2_thresh` 到 0.5/0.6，看保留因子数量和对回测的影响
3. **其他品种**：参数确定后，复制到 M/P/Y 的 notebook

### 已知陷阱（新增）

1. **matplotlib legend 参数名是 `prop`**，不是 `fontproperties` — 用错会导致字体不生效
2. **notebook JSON 中 f-string 不能跨行** — 任何包含 `\n` 的 f-string 在 JSON 序列化后会被拆成两行，导致 SyntaxError
3. **文泉驿正黑字体路径**: `/usr/share/fonts/truetype/wqy/wqy-zenhei.ttc`

### 路径速查（更新）

| 用途 | 路径 |
|------|------|
| 月度稳定性 notebook（最终版 Cell 5） | `/home/strategy_res/single/dce_农/20250701/A_2025-07-01_v0_monthly_stability.ipynb` |
| 中文字体 | `/usr/share/fonts/truetype/wqy/wqy-zenhei.ttc` |
| A 品种预训练输出 | `/mnt/Data/writable/liaoyuyang/factor_eval_commodity/2025-07-01/` |

> 用户已下班，明日继续。
