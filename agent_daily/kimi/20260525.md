# Agent 状态简报 — 2026-05-25

> 这是给下一个 agent 看的。读完你就知道该干什么，不用重头问用户。

## 当前进度
- 回放模式（replay_mode）debug 中
- `trade_date` 已修复（代码层面）
- `run_345` 前十分钟开仓异常正在排查（尚未定位）

## 今天做了什么（代码变更摘要）

### 修改 1：`sql_writer_dce.py` — replay_mode 下 trade_date 固定为 day_date
- **问题**：夜盘 tick（hour≥18）的 `trade_date` 被错误设为 `night_date`（2026-03-23），导致 `day_jump` 等日线因子 groupby 时把夜盘和日盘分到不同交易日
- **根因**：`_handle_market_data` 中 replay_mode 启用时，`trade_date = tick_datetime.strftime('%Y-%m-%d')`，而 `tick_datetime` 已被按 hour 替换为 `night_date`
- **修复**：replay_mode 下 `trade_date` 统一用 `self.replay_mode.get('day_date')`，不再随 hour 变化
- **文件位置**：`/home/strategy_PAMY_dev/sql_writer_dce.py`（约第 792 行）
- **注意**：`tick_data.db` 中已有错误数据（`trade_date=2026-03-23` 的夜盘记录），**用户未确认是否清理**

### 排查 1：`run_345` — 回放夜盘 21:01 即开仓，违反"前十分钟未开仓"规则
- **现象**：market time 21:01:00 时，A/B/CS 已显示 pos=1, hold=1
- **预期**：21:10 前应该返回 `signal='未开仓'`
- **排查状态**：已读取 `run_345` 源码，其末尾有 `in_night_session = (night_start < current_time <= night_end)` 判断，逻辑看起来正确。但用户说"以前都是对的，这个应该是刚刚才改错了的"
- **未定位**：尚不清楚是哪个 recent change 导致该逻辑失效

## 关键待办（按优先级）
- [ ] **P0** 定位 `run_345` 前十分钟开仓异常的根因（用户怀疑最近某次改动引入）
- [ ] **P1** 确认是否清理 `tick_data.db` 中 `trade_date=2026-03-23` 的错误记录
- [ ] **P2** 回放验证 `trade_date` 修复后 `day_jump` 等因子是否对齐

## 如果你要继续做，建议顺序
1. 先问用户："`run_345` 前十分钟不开仓的规则，最近改动了哪些文件？" 或让用户确认排查方向
2. 检查 git diff / 最近修改的文件，定位可能破坏 `run_345` 时间判断的改动
3. `trade_date` 清理需等用户明确指令

## 已知陷阱（别踩）
- `trade_date` 语义：replay_mode 下必须固定为 `day_date`，与 hour 无关（用户明确口授）
- `run_345` 的时间过滤逻辑可能受 `df.index` 类型或 `valid_index` 构造方式影响
- 用户强调：**意义不明确的操作不要自己判断，一定要先问**

## 路径速查
| 用途 | 路径 |
|------|------|
| trade_date 修复 | `/home/strategy_PAMY_dev/sql_writer_dce.py` |
| 策略逻辑 / run_345 | `/home/strategy_PAMY_dev/data_function.py` (line 3582) |
| 策略类 / run_logic | `/home/strategy_PAMY_dev/strategies.py` (line 1513) |
| 编排器 | `/home/strategy_PAMY_dev/orchestrator.py` |
| tick DB | `/home/strategy_PAMY_dev/tick_data.db` |
| config | `/home/strategy_PAMY_dev/config.json` |
| 实时日志 | `/home/strategy_PAMY_dev/logs/orchestrator_20260525.log` |
