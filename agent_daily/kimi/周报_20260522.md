# 周报 2026-05-22

## 本周工作

### 1. P 品种实时回放因子差异排查
- 定位到问题在数据切割层：DataService 的 `min_cache` 被 `aggregate_ticks` + `update_concat` 覆盖，导致传给策略的 min 数据与正确值不符
- `RPP_5D` 差异根源：logger 中 21:37:00 close=9724，save_files 中 close=9710
- 部署 21:15 快照断点调试：在 `data_service.py` 和 `strategies.py` 同时保存各层数据，方便离线对比

### 2. 数据工程
- **rebuild_level2_all 化工版**：按有色版模板生成化工版本，品种列表 `[“RU”, “BR”, “NR”, “L”, “TA”, “PF”, “V”, “EG”, “MA”, “PP”, “EB”, “UR”, “SA”, “SH”, “PX”, “PR”, “FG”]`
- **calc_factors 多版本统一**：将贵金属、黑色、农产品类、油脂油料、有色、czce农、化工 7 个版本的核心逻辑统一为 dce农 标准：
  - `latest_instrument_next` 直接从 `df_symbol['instrument_next']` 取
  - `data1m_next` 统一用空 DataFrame
  - `instruments_lst` 不截断最后一张合约

### 3. 调试工具
- 修复了 21:15 快照中 `Path` 未导入的问题（`data_service.py` 和 `strategies.py` 双处）
- 修复了子进程 `sys.exit(0)` 导致主进程报异常的问题，改为子进程正常返回、主进程统一退出

## 下周计划

1. **跑通 21:15 快照**，对比 DataService 层与策略层的 `tick_cache` / `min_cache` / `fac_generator` 状态，确认数据切割问题的具体环节
2. **修复数据切割 bug**：根据快照结果调整 `_refresh_instrument` 或 `update_concat` 逻辑
3. **化工版本数据跑批**：运行 `rebuild_level2_all_化工.py` 和 `calc_factors_化工.py`

## 待跟进问题

- [ ] P 品种 `last` 后缀因子错误的根因确认
- [ ] `RPP_5D` 误差 > 5% 的具体原因
- [ ] LH 旧缓存数据更新（已修复 multiplier=10 → 16）
