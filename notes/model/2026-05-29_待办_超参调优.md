# 待办：LightGBM 超参调优

## 背景

昨晚 `experiment_tricks.py` 8 方案批量实验结论已出：
- **损失函数按品种配置** 是最显著改进（C/LH → MSE，其余 → Huber）
- 标签平滑、方向敏感损失、分位数权重均已排除

## 当前默认超参

```python
learning_rate=0.005
num_leaves=32
max_depth=5
min_data_in_leaf=500
lambda_l1=1
lambda_l2=1
feature_fraction=0.7
bagging_freq=10
extra_trees=True
max_bin=32
num_threads=20
```

## 待办事项

### 1. 【高优先级】先落地按品种配置损失函数
- 修改 `pipeline_v1.py` / notebook 模板，让 `LGB_PARAMS` 按品种读取 `LOSS_CONFIG`
- 用新配置训练一版 8 品种 5-fold，确认综合 test_corr 提升

### 2. 【中优先级】轻量级超参搜索
在损失函数已落地的基础上，对 **A 或 Y 品种** 跑一个 3×3 网格搜索：

| 参数 | 搜索空间 |
|------|---------|
| `learning_rate` | [0.003, 0.005, 0.01] |
| `num_leaves` | [16, 32, 64] |

其他参数固定，目标指标：`test_corr`。

**预期工作量**：9 组 × 单品种 5-fold ≈ 15~20 分钟。

### 3. 如果网格搜索有效，再考虑扩展
- `min_data_in_leaf`: [300, 500, 1000]（尤其 LH 数据量少，可能需要调大）
- `lambda_l1/l2`: [0.1, 1, 10]
- `feature_fraction`: [0.6, 0.7, 0.8]

## 已知约束

- `experiment_tricks.py` 单核顺序执行，不同超参之间不要手动并行（会爆内存）
- `strategy_res/single/` 在 `.gitignore` 里，新增脚本需 `git add -f`
- 当前 `high_conf_dir_acc` 阈值 0.9 不合理，修复前不要用做搜索目标

## 相关文件

- 实验脚本：`/home/strategy_res/single/dce_农/20250701/experiment_tricks.py`
- 训练 pipeline：`/home/strategy_res/single/dce_农/20250701/pipeline_v1.py`
- 默认超参定义：同文件第 80~98 行
