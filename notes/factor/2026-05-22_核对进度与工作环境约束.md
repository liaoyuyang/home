# 2026-05-22 核对进度与工作环境约束

## 用户口头确认的关键约束

1. **历史缓存回存整个交易日数据**，但实时回放时**不放 LH**（夜盘）。factor_cache 里的 LH 是旧白天盘缓存。
2. **`all_factor.feather` 是旧代码生成**的，不要假设它有 10ms 去重或 mid_price.round(4)。
3. **10ms 去重意义不大**，用户 tick 精度到不了 10ms。
4. **遇到问题先问用户**，不擅自改代码。

## 今日核对结果（factor_cache 2026-03-23 vs all_factor.feather）

| 品种 | 状态 | 问题因子 |
|------|------|----------|
| A, B, CS, P | ✅ 全部正常 | 无 |
| C | ⚠️ skewness 类差异 | `FAC_VWAP_Deviation_skewness` 47%, `FAC_SPREAD_skewness` 46%, `FAC_DBCD_skewness` 44% |
| M | ⚠️ 1个 | `FAC_Depth_Reversal_skewness` 12%（有效点仅25个） |
| Y | ⚠️ 1个 | `FAC_Depth_Reversal_skewness` 12% |
| LH | ❌ 严重差异 | `FAC_MPB_*` 100% fail (max_diff 6000+), `zigzag` 17% fail |

**结论**：
- `FAC_bid_amount_sub20` 三因子值相同的问题在 factor_cache 中**未复现**，可能已经修复。
- C/M/Y 的 `skewness` 差异与已知的 **night 数据日期归属不一致** 有关（SOP 已记录）。
- LH 的 `MPB` 差异需先确认数据来源（旧缓存 vs 新回放），再决定是否需要排查。

## 待用户确认

- [ ] LH 的 factor_cache 数据是哪天生成的？是否需要用最新白天盘数据重新生成？
- [ ] skewness 差异是否接受（已知是 night 数据日期归属问题，非代码 bug）？
- [ ] 是否需要用最新代码重新生成 `all_factor.feather`？
